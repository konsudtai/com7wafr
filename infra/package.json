{
  "name": "wa-review-infra",
  "version": "0.1.0",
  "description": "AWS CDK infrastructure for Well-Architected Review Tool",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy --all"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.100.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "aws-cdk": "^2.100.0",
    "typescript": "~5.4.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  }
}
